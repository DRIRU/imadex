# Project instructions

- Keep `plan.md` updated with the current plan, progress, verification results, and remaining work. The user explicitly requested this on 2026-09-23.
- This file is the canonical source of shared project instructions. Maintain `GEMINI.md`, `CLAUDE.md`, and `OPENCODE.md` as pointers to this file and `plan.md`, avoiding duplicated rules.
- Use `.venv/Scripts/python.exe` for running the app and tests.
- Preserve original Drive images. Embedding inference and vector storage run locally.
- Preserve the mobile layout and authenticated tunnel access.
- Preserve concurrent user edits; the project was renamed from `index` to `imadex` during implementation.
